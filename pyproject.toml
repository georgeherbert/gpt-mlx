[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
mlx = "^0.9.0"

[tool.black]
line-length = 120

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
